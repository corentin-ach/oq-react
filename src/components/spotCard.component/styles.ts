const styles = {
  mainCard: {
    bgcolor: 'background.default',
    border: 0,
    borderRadius: 5,
    padding: 2,
    alignContent: 'flex-start',
    display: 'flex',
    flexDirection: 'column',
  },
} as const;

export default styles;
