const styles = {
  toggleButton: {
    height: 50,
    width: 50,
    position: 'absolute',
    borderRadius: 2,
    top: 0,
    right: '20%',
    margin: 5,
    zIndex: 10,
  },
} as const;

export default styles;
