const styles = {
  container: {
    bgcolor: 'background.default',
    padding: 4,
    alignContent: 'flex-start',
    display: 'flex',
    flexDirection: 'column',
  },
} as const;

export default styles;
