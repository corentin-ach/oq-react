const styles = {
  loader: {
    height: 50,
    width: 50,
    position: 'absolute',
    borderRadius: 2,
    top: '30%',
    right: '45%',
    margin: 5,
    zIndex: 10,
  },
} as const;

export default styles;
