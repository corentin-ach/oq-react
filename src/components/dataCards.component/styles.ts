const styles = {
  dataCards: {
    display: 'flex',
    height: '75%',
    flexDirection: 'column',
    zIndex: 10,
    position: 'absolute',
    top: 0,
    left: 0,
    margin: 5,
    justifyContent: 'space-between',
  },
} as const;

export default styles;
