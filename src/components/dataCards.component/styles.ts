const styles = {
  dataCards: {
    display: 'flex',
    height: '75%',
    flexDirection: 'column',
    zIndex: 10,
    position: 'absolute',
    top: 60,
    left: 0,
    margin: 5,
  },
} as const;

export default styles;
