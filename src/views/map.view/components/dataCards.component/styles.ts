const styles = {
  dataCards: {
    display: 'flex',
    height: '75%',
    flexDirection: 'column',
    // zIndex: 10,
    position: 'absolute',
    top: 60,
    left: 0,
    margin: 5,
  },
  question: {
    color: 'text.primary',
    fontSize: 12,
    fontWeight: 'bold',
  },
} as const;

export default styles;
